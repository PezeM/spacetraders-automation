{
  "name": "spacetraders-test",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && xcopy /e /k /i /y \"config\" \"dist/config\"",
    "watch": "tsc --watch",
    "start:prod": "yarn build && cd dist && cross-env NODE_ENV=production node index.js",
    "start:dev": "yarn build && cd dist && cross-env NODE_ENV=development node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "config": "^3.3.4",
    "spacetraders-api-sdk": "^0.0.12",
    "typescript": "^4.2.3"
  },
  "devDependencies": {
    "@types/config": "^0.0.38",
    "@types/node": "^14.14.31",
    "cross-env": "^7.0.3"
  }
}
